{
  "name": "linc-server",
  "version": "1.0.0",
  "description": "Linc global leaderboard server (and optional AI key proxy)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
